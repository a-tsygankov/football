{
  "name": "@fc26/squad-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run typecheck",
    "test:run": "pnpm run typecheck",
    "sync": "pnpm run build && node dist/main.js"
  },
  "dependencies": {
    "@fc26/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
